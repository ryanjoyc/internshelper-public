# Python
__pycache__/
*.pyc
*.egg-info/
build/
dist/
.pytest_cache/

# Virtualenv
.venv/
venv/

# App data (SQLite archive lives here)
data/

# Secrets — the realized launchd plist holds the SMTP password in plaintext.
# Only the committed *.plist.template is tracked.
*.plist
.env
secrets*
